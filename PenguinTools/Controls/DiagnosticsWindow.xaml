<Window
    Height="600"
    MinHeight="300"
    MinWidth="400"
    Style="{DynamicResource DialogWindowStyle}"
    Title="{Binding Title}"
    Width="800"
    WindowStartupLocation="CenterOwner"
    d:DataContext="{d:DesignInstance controls:DiagnosticsWindowViewModel}"
    mc:Ignorable="d"
    x:Class="PenguinTools.Controls.DiagnosticsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:behaviors="clr-namespace:PenguinTools.Behaviors"
    xmlns:controls="clr-namespace:PenguinTools.Controls"
    xmlns:converters="clr-namespace:PenguinTools.Converters"
    xmlns:core="clr-namespace:PenguinTools.Core;assembly=PenguinTools.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:res="clr-namespace:PenguinTools.Core.Resources;assembly=PenguinTools.Core"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Window.Resources>
        <converters:PercentageConverter x:Key="PercentageConverter" />
    </Window.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel
            Grid.Row="0"
            Margin="8"
            VerticalAlignment="Top">

            <ListView
                AlternationCount="1000"
                ItemsSource="{Binding Diagnostics}"
                MaxHeight="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource PercentageConverter}, ConverterParameter=0.6}"
                MinHeight="100"
                ScrollViewer.CanContentScroll="True"
                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                SelectedItem="{Binding SelectedDiagnostic}"
                SelectionMode="Single"
                VerticalAlignment="Top">

                <ListView.View>
                    <GridView>
                        <GridViewColumn Width="Auto">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Image
                                        Height="12"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Width="12">
                                        <Image.Style>
                                            <Style TargetType="Image">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Severity}" Value="{x:Static core:Severity.Error}">
                                                        <Setter Property="Source" Value="{StaticResource ErrorIcon}" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Severity}" Value="{x:Static core:Severity.Warning}">
                                                        <Setter Property="Source" Value="{StaticResource WarningIcon}" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Severity}" Value="{x:Static core:Severity.Information}">
                                                        <Setter Property="Source" Value="{StaticResource InfoIcon}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Image.Style>
                                    </Image>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="{x:Static res:Strings.Label_Time}" Width="{Binding TimeColumnWidth}">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock
                                        HorizontalAlignment="Center"
                                        TextWrapping="Wrap"
                                        VerticalAlignment="Center">
                                        <TextBlock Text="{Binding FormattedTime}" />
                                    </TextBlock>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="{x:Static res:Strings.Label_Path}" Width="{Binding PathColumnWidth}">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock
                                        HorizontalAlignment="Center"
                                        Text="{Binding Path}"
                                        TextWrapping="Wrap"
                                        VerticalAlignment="Center" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="{x:Static res:Strings.Label_Message}">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock
                                        HorizontalAlignment="Left"
                                        Text="{Binding Message}"
                                        TextWrapping="Wrap"
                                        VerticalAlignment="Center" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </DockPanel>

        <GroupBox
            Grid.Row="1"
            Header="{x:Static res:Strings.Label_Details}"
            Margin="8,0,8,0"
            MaxHeight="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource PercentageConverter}, ConverterParameter=0.4}"
            MinHeight="100">
            <i:Interaction.Behaviors>
                <behaviors:HideIfNullValueBehavior Value="{Binding SelectedDiagnostic}" />
            </i:Interaction.Behaviors>
            <controls:ObjectTreeView SelectedObject="{Binding SelectedDiagnostic}" />
        </GroupBox>

        <controls:Alert
            Grid.Row="2"
            Level="Information"
            Margin="0,8,0,0"
            Message="{x:Static res:Strings.Alert_Diagnostic_report}" />

        <StackPanel Grid.Row="3" Style="{StaticResource ActionPanelStyle}">
            <Button Content="OK" IsDefault="True">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <i:CallMethodAction MethodName="Close" TargetObject="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
        </StackPanel>
    </Grid>
</Window>