<Window
    Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
    DataContext="{Binding RelativeSource={RelativeSource Self}}"
    Height="400"
    MaxHeight="900"
    MaxWidth="1200"
    MinHeight="300"
    MinWidth="450"
    ResizeMode="CanResize"
    SnapsToDevicePixels="True"
    TextOptions.TextFormattingMode="Display"
    Title="{x:Static res:Strings.Title_Error}"
    Width="600"
    WindowStartupLocation="CenterScreen"
    WindowStyle="SingleBorderWindow"
    mc:Ignorable="d"
    x:Class="PenguinTools.Views.ExceptionWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:res="clr-namespace:PenguinTools.Resources"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="8,0,0,0" />
            <Setter Property="Padding" Value="12,4" />
            <Setter Property="MinWidth" Value="80" />
            <Setter Property="MinHeight" Value="28" />
        </Style>

        <Style TargetType="TextBlock" x:Key="ErrorHeaderStyle">
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#D32F2F" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel
            Grid.Row="0"
            HorizontalAlignment="Left"
            Margin="-32,0,0,12"
            Orientation="Horizontal">
            <Image
                Height="24"
                Margin="0,0,8,0"
                Source="{StaticResource ErrorIcon}"
                Width="24" />
            <TextBlock Style="{StaticResource ErrorHeaderStyle}" Text="{x:Static res:Strings.Error_Unhandled}" />
        </StackPanel>

        <Border
            Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
            BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
            BorderThickness="1"
            CornerRadius="2"
            Grid.Row="1"
            Margin="0,0,0,12">
            <ScrollViewer
                Focusable="False"
                HorizontalScrollBarVisibility="Auto"
                Padding="4"
                VerticalScrollBarVisibility="Auto">
                <TextBox
                    Background="Transparent"
                    BorderThickness="0"
                    FontFamily="Consolas"
                    FontSize="13"
                    HorizontalScrollBarVisibility="Auto"
                    IsReadOnly="True"
                    IsReadOnlyCaretVisible="True"
                    Text="{Binding StackTrace}"
                    TextWrapping="Wrap"
                    VerticalScrollBarVisibility="Auto" />
            </ScrollViewer>
        </Border>

        <DockPanel Grid.Row="2" HorizontalAlignment="Right">
            <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                <Button Click="CopyButton_Click" Content="{x:Static res:Strings.Button_Copy_Exception}" />
            </StackPanel>

            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button
                    Click="CloseButton_Click"
                    Content="{x:Static res:Strings.Button_Close}"
                    IsCancel="True"
                    IsDefault="True"
                    MinWidth="90" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>